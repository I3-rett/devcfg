version: "2"

linters:
  enable:
    - govet
    - gocyclo
    - ineffassign
    - misspell
    - revive

  settings:
    gocyclo:
      min-complexity: 15
    revive:
      rules:
        - name: exported

formatters:
  enable:
    - gofmt
